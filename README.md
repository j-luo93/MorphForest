# Unsupervised Learning of Morphological Forest 
* need Theano and Gurobi solver to run the model
* `py` folder contains source files
* run `test.sh` to train the model, though only a toy word vector file is used.
